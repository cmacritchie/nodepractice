{
  "name": "project1",
  "version": "1.0.0",
  "description": "modules are units, used using 'require'",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Craig Mac",
  "license": "ISC",
  "dependencies": {
    "load": "^1.0.2"
  }
}
